# 🌦 Weather Report Helper

**Инструмент для подготовки отчёта по погоде для велопоходов и других outdoor-мероприятий.**

Вы получаете интерактивный дашборд с исторической погодой (температура, осадки, ветер) для всех ключевых точек маршрута за указанные годы и часы.

## Что вы получите

- **CSV-таблицу** со всеми данными.
- **Интерактивный HTML-дашборд** с фильтрами (по дате, типу точки, названию) и графиками:
  - изменение температуры в течение дня (для выбранной точки),
  - осадки и ветер (столбцы и линии).

## Как это работает

1. Вы формируете таблицу точек маршрута (дата, название, координаты, высота) – это можно сделать вручную, но проще доверить это DeepSeek (или другому ИИ).
2. Вы запускаете Python-скрипт, который:
   - читает таблицу,
   - запрашивает историческую погоду через Open-Meteo API,
   - добавляет данные в таблицу,
   - строит дашборд.

---

## 1. Подготовка данных

### Вариант А (рекомендуемый) – через DeepSeek

- Скопируйте следующий запрос в чат DeepSeek и приложите к нему **четыре файла**:
  - план похода (Markdown),
  - `ночевки.txt` (GPX),
  - `населенка.txt` (GPX),
  - `ПП.txt` (GPX).

**Запрос:**
```
Составь таблицу всех точек маршрута (населённые пункты, места ночёвок, перевалы) с привязкой к датам, координатам и высотам.

Правила:
1. Дата – из плана (строки вида «ДЕНЬ X_ДД.ММ.ГГГГ»). Для ночёвок дата указана в названии (мс21-07 → 21.07.2026). Для остальных – по дню упоминания в плане.
2. Название точки – из плана.
3. Координаты: для населённых пунктов – из населенка.txt, для ночёвок – из ночевки.txt, для перевалов – из ПП.txt. Если не найдено – оставить пустыми.
4. Высота: для ночёвок и перевалов – из плана (в скобках). Для населённых пунктов – можно оставить пустой.

Формат вывода – CSV с разделителем «;» и колонками:
date;name;type;lat;lon;height
```

DeepSeek вернёт CSV-текст. Скопируйте его и сохраните как **`points.csv`** (в кодировке UTF-8) в папку, где лежит скрипт.

### Вариант Б – вручную

Вы можете создать CSV‑файл самостоятельно, соблюдая тот же формат. Все поля обязательны (кроме `height`).

---

## 2. Установка

1. Скачайте или склонируйте репозиторий.
2. Убедитесь, что у вас установлен **Python 3.8+**.
3. Установите необходимые библиотеки:
```bash
pip install pandas requests
```

---

## 3. Запуск

1. Поместите файл `points.csv` в ту же папку, что и скрипт `weather_report_helper.py`.
2. Откройте `weather_report_helper.py` в любом текстовом редакторе и убедитесь, что в нём раскомментирована (не закомментирована) строка:
```python
EXISTING_CSV = "points.csv"
```
Если вы используете другое имя файла – укажите его.

3. В настройках скрипта можно изменить:
   - `YEARS` – список лет для анализа (например, `[2024, 2025]`),
   - `HOURS` – какие часы (местное время) запрашивать: `[0, 9, 13, 18]`.

4. Запустите скрипт:
```bash
python weather_report_helper.py
```

Скрипт будет по очереди запрашивать погоду для каждой точки и каждого года. **Это может занять некоторое время** (на каждую точку и год ~1–2 секунды). Наберитесь терпения.

---

## 4. Результаты

После завершения работы в папке `output/` вы найдёте:

- **`points_full.csv`** – полная таблица со всеми добавленными данными (температура, осадки, ветер по годам, min/max).
- **`dashboard.html`** – интерактивный дашборд. Откройте его в любом браузере.

**В дашборде можно:**
- фильтровать по дате, типу точки (ночёвка/перевал/населённый пункт) и конкретному названию,
- смотреть графики температуры, осадков и ветра для выбранной точки,
- видеть цветную таблицу с экстремумами.

---

## 5. Настройка под новый поход

Для другого похода достаточно:
1. Подготовить новый CSV-файл по тем же правилам.
2. (Опционально) изменить `YEARS` и `HOURS`.
3. Запустить скрипт с этим CSV.

---

## 6. Возможные проблемы и их решение

| Проблема | Решение |
|----------|---------|
| **Ошибка IndentationError** | Проверьте, что в строке `EXISTING_CSV = "points.csv"` нет лишних пробелов перед ней. |
| **Ошибка при чтении CSV** | Убедитесь, что файл сохранён в кодировке **UTF-8** и разделитель – точка с запятой `;`. |
| **Долго выполняется** | Уменьшите количество лет в `YEARS` (например, оставьте только 2025). Можно также запускать скрипт ночью. |
| **Нет данных для некоторых точек** | Проверьте, что у них есть дата и координаты. Если дата отсутствует – точка будет пропущена. |
| **API ошибки (500, 429)** | Скрипт автоматически делает паузы между запросами. При частых ошибках попробуйте позже, сервис бесплатный и может временно ограничивать. |

---

## 7. Пример записи в CSV

```
date;name;type;lat;lon;height
21.07.2026;ТАЛАС;населённый пункт;42.510955;72.273736;
22.07.2026;мс2;ночёвка;42.211771;72.422909;2675
23.07.2026;Пер. Терек;перевал;42.201214;72.363171;3376
```

---
## 7. Пример анализа данных
Полученные данные в файле .csv можно использовать для анализа с помощью ИИ. Вот пример запроса:
```
Какие рекомендации можно дать участникам похода исходя из полученных данных

```
Пример результата анализа:

```
Что показал анализ погоды за 2024–2025


1️⃣ Температура: разброс огромный
По дням: в одни и те же числа июля-августа температура в одной и той же точке в 2024 и 2025 годах отличалась на 5–10 градусов. То есть полагаться на прошлогодние ощущения не стоит – год на год не приходится.
Перепады в течение дня существенные. В долинах утром +18, в обед уже +35 – разница почти 20 градусов. На перевалах утром +2, днём +12 – перепад меньше, но всё происходит при низких температурах. И то и другое требует внимания к одежде: в долинах надо спасаться от жары, на перевалах – от холода.


2️⃣ Ветер – главное, что может испортить день
На перевалах Терек, Сары-Кыр, Кегеты, Кок-Айрык в отдельные дни ветер достигал 14–15 м/с. Это уже серьёзно: ветер заметно холодит, мешает держать равновесие, выматывает. По ощущениям из-за ветра температура кажется на 5–7 градусов ниже.


3️⃣ Осадки: дожди есть, но не фатальные
В большинство дней за два года осадков не было. Но в отдельные дни шли дожди, иногда с грозами. Чаще всего – во второй половине дня.
Осадков выпадало немного – до 2–3 мм в час. Но это в горах, где любая вода быстро охлаждает и может застать врасплох.
Лучше проходить перевалы в первой половине дня – до обеда вероятность дождя ниже.


4️⃣ Ночи будут холоднее, чем кажутся
Вот конкретные цифры по стоянкам (это температура в 0:00, минимальная ночная за два года):
• мс2 (2675 м) – ночью около +10°C (в 2024 было 11.2, в 2025 – 10.3). Без ветра терпимо, но с ветром – прохладно.
• мс13 (3189 м) – ночью около +4.6°C (в 2024 было 10.4, в 2025 – 4.6). Спальник нужен тёплый.
• мс17 (3300 м) – ночью около +1.5°C (в 2024 было 6.4, в 2025 – 1.5). Практически ноль. Без хорошего спальника и тёплой одежды будет совсем некомфортно.
• Пер. Кегеты (3805 м) – если придётся там ночевать, ночью будет около +0.4°C (в 2024 было 4.4, в 2025 – 0.4). Это уже почти заморозки.
• Пер. Кок-Айрык (3890 м) – ночью около 0°C (в 2024 было 0.8, в 2025 – -0.6). С ветром будет значительно холоднее.
Вывод: спальник должен быть с запасом – ориентируйтесь на комфорт 0°C или даже -5°C. 


5️⃣ Днём в долинах – настоящая жара
Токтогул, Казарман, Орловка – в этих местах днём было +35…+37°C в отдельные дни. Это серьёзно.

Что делать:
– Питьевой режим – главное. Без воды в такую жару на велосипеде делать нечего. Изотоник пригодится.
– Солнце в горах гораздо агрессивнее, чем кажется. Крем, очки, бандана – обязательно.
```

---

## 8. Лицензия

Свободно используйте и модифицируйте для любых некоммерческих целей.

---

**Вопросы и предложения** – создавайте issue в репозитории.
